info:
  name: AtualizarPersonal
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5098/api/personal
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
